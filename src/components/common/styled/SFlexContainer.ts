import styled from 'styled-components'

export const SFlexRow = styled.div`
    display: flex;
    flex-direction: row;
    flex-wrap: nowrap;
    
`

export const SFlexRowWrap = styled.div`
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
`

export const SFlexCol = styled.div`
    display: flex;
    flex-direction: column;
    align-items: center;
`